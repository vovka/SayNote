{
  "name": "@saynote/backend",
  "private": true,
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json && tsc --noEmit -p tsconfig.test.json",
    "worker": "node dist/backend/worker/index.js",
    "test": "node --test --experimental-strip-types $(find worker -name '*.test.ts' -print)"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.1025.0",
    "pg": "^8.16.3",
    "zod": "^3.24.2"
  },
  "devDependencies": {
    "@types/node": "^22.13.8",
    "@types/pg": "^8.15.5",
    "typescript": "^5.8.2"
  }
}
